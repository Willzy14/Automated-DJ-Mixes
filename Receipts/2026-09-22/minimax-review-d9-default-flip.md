VERDICT: SOUND

## Findings

**(1) Default flip only adds RESCUE-tier fallback anchors.** `align_engine.py::_incoming_swap_anchors` (line ~1157) always calls `_incoming_drop_anchors` first and only conditionally adds `_incoming_phrase_anchors` and `_deep_intro_anchor`. `_align_pair_landmark_aware` (line ~1190) runs `_search_anchors` with `drop_anchors` (rank_all=False) FIRST; if that returns None it tries `rescue_anchors` (phrase+deep, rank_all=True), and only THEN `_search_tail_anchor_rescue` if `tail_anchor_rescue` is True. Invariant: turning the three rescue-tier flags ON can only convert `raise → ok`, never move an already-OK pair or break a primary drop-anchor search. d9_replay_result.json confirms empirically (`changed: []`, `newly_raise: []`, 87 newly_ok rows = 354 - 267).

**(2) Flag-leak coverage from retired baseline test is preserved.** test_codex_blocker_fixes.py Fix 3 (`test_rescued_pair_with_flags_off_gets_no_loop_or_cut`) directly constructs an `Alignment(alignment_policy="tail_anchor_rescue_v1")` and asserts `plan_fill_or_cut` returns no specs with all flags off — exercising the exact flag-leak class the retired `compute_rescue_rows` guarded. The prove-the-test sibling (`test_fixture_is_potent_legacy_policy_does_loop`) confirms the same geometry under `legacy_v1` DOES emit an intro loop, so the empty-specs assertion is meaningful, not vacuous. Independently of the corpus sweep or ambient CUE_CONFIG.

**(3) 267→353 arithmetic verified.** `d9_replay_result.json`: `default_ok: 267`, `combined_ok: 354`, `newly_ok: 87` (87 of the 113 default raises newly align). Of the 87, exactly 1 has `status:"ok"` with `plan.status:"raise"` — the `Ritmo Da Rua - Harry Romero Remix 24 Bit MASTER -> Christoph - The Rise 16 Bit MASTER` pair (line 15657, alignment_policy `tail_anchor_rescue_v1`, handoff169, overlap 48, plan raises "Cannot plan outgoing tail loop ... cue 'section:break_1' would end 2"). feasible() returns False for it. 354-1 = 353. Math holds. The only nested `plan.status:"raise"` in combined_rows is this one pair.

**(4) test_intro_phrase_swaps.py is purely a fixture-completeness fix.** The SimpleNamespace stub needed `n_bars` (for `_mix_cues`'s `add(track.n_bars, "track_end", 5)`) and `musical_landmarks` (for the `for landmark in track.musical_landmarks` loop) once D9 made the rescue tier the default and `_incoming_swap_anchors` unconditionally reaches `_mix_cues`. All 10 test invocations' assertion logic is byte-identical; only the helper signature changed and one docstring was updated to acknowledge the new default. Falsifier would be: any of `_incoming_drop_anchors`/`incoming_phrase_anchors`/`incoming_swap_anchors` assertions now exercise different code paths — none do.

**(5) test_policy_threading.py autouse fixture scope is correct.** It pins the three D9-affected flags to all-off at file scope because the file tests an orthogonal concern (policy-CAP threading). With D9 default ON, the rescue tier could find an alternative anchor for a deliberately-tightened test policy, masking whether the cap was actually read. Scope is right: not overkill (one module-state, every test needs the same isolation), not underkill (the four other CueConfig flags remain False and unchanged by D9). Each of the 6 tests is independent of the rescue tier's behaviour, so the isolation is necessary and sufficient.

**(6) test_tail_anchor_rescue.py rework is docstring/naming honesty only.** Five tests, no logic changes. `_with_flag` uses `dc_replace(saved, tail_anchor_rescue=flag_value)`, so the saved CUE_CONFIG's deep_intro_anchor/incoming_phrase_anchors are preserved — meaning `test_flag_on_unblocks_revoloution_to_renegades` legitimately depends on D9's default (deep/phrase ON, rescue toggleable) for its "ON aligns, OFF raises" assertion to hold. The new docstring's note "(with `deep_intro_anchor`/`incoming_phrase_anchors` left at their (now-default) True)" correctly documents this dependency. No assertions moved.

**(7) Nothing in propose_arrangement.py or the test files assumes old defaults.** Sole CUE_CONFIG references in propose_arrangement.py: `_apply_cue_signals` CLI mapping (untouched, all 8 tokens still mapped to their canonical flag names) and one docstring reference to `emit_hint_fields`. No code path reads `tail_anchor_rescue`/`deep_intro_anchor`/`incoming_phrase_anchors`. The CueConfig dataclass docstring itself (line 168) explicitly states "Three are the exception ... default ON as of 2026-09-22", so any future reader gets the context.

===REVIEW-COMPLETE===


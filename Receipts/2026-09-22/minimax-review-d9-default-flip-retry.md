VERDICT: SOUND

(1) **Default flip rescue-only — invariant holds.** `align_engine._align_pair_landmark_aware` (lines 1170-1217): the `_search_anchors(drop_anchors, ...)` call (primary path, drop anchors only) runs unconditionally; the two rescue tiers (`rescue_anchors` set update + `_search_tail_anchor_rescue`) only fire when `chosen is None`. `_incoming_swap_anchors` (lines 1149-1166) only adds non-drop anchors via `incoming_phrase_anchors`/`deep_intro_anchor` flags. With the flag flip, the rescue tier set is wider but `drop_anchors` (the primary path) is unchanged → already-OK pairs cannot move. `d9_replay_result.json:changed` is `[]` — empirical confirmation. Falsifier for the invariant: "any post-D9 already-OK pair with a different `handoff_bar_out`/`arr_offset_bars`/`overlap_bars`/`alignment_policy` than pre-D9." None observed in 380 pairs.

(2) **Flag-leak coverage preserved in `test_codex_blocker_fixes.py` Fix 3.** `test_rescued_pair_with_flags_off_gets_no_loop_or_cut` (lines 113-120) constructs the Alignment directly with `alignment_policy="tail_anchor_rescue_v1"` and asserts `specs == []`; `test_fixture_is_potent_legacy_policy_does_loop` (lines 123-132) proves the same geometry under `legacy_v1` fires `incoming_intro`. Independent of corpus sweep and independent of ambient CUE_CONFIG — the test correctly guards the `plan_fill_or_cut` classification at the source.

(3) **267→353 arithmetic checks out against `d9_replay_result.json`.** Top-level: `combined_ok=354, combined_raised=26, changed=[]`, total 380. Pre-D9 OK = 354 - 87 (rescued) = 267 ✓. The "Ritmo Da Rua - Harry Romero Remix 24 Bit MASTER → Christoph - The Rise 16 Bit MASTER" row at ~line 38196 has `status: "ok"` with `alignment_policy: "tail_anchor_rescue_v1"`, but `plan: {status: "raise", error: "ValueError", msg: "Cannot plan outgoing tail loop ... cue 'section:break_1' would end 2..."}` — so feasible() = 354 - 1 = 353 = 267 + 87 - 1 ✓.

(4) **`test_intro_phrase_swaps.py` — fixture completeness only, no assertion logic change.** The `_track()` helper's `SimpleNamespace` now carries `n_bars` (defaults to max section end) and `musical_landmarks=[]` because post-D9 `_incoming_swap_anchors` unconditionally calls `_mix_cues` (via `_incoming_phrase_anchors`), which reads both. The 9 test functions (10 cases including the parametrized one at line 112) retain identical assertions to the pre-D9 versions.

(5) **`test_policy_threading.py` autouse fixture — slightly broader than strictly needed, not wrong.** The fixture isolates CUE_CONFIG to pre-D9 all-off for the whole file. Necessary for `test_a_tighter_policy_genuinely_rejects_the_same_overlap` (lines 113-126), where D9 rescue tiers could otherwise rescue the same-overlap fixture and mask the policy-cap threading test. Overkill for the three `pick_cue_bounded_drum_loop_*` tests (which only read `policy.max_loop_repeats`, never CUE_CONFIG), but a single file-wide autouse fixture is the safer shape than splitting scope. SEVERITY: MINOR — could be scoped per-test but current shape is correct.

(6) **`test_tail_anchor_rescue.py` — no logic change beyond honest documentation.** All 5 test functions pin the same conditions as before. Docstring rewritten to explain that D9 changed the DEFAULT, not the CODE. Critically, `test_all_three_flags_off_reproduces_the_original_267_113_split` (line 76) explicitly sets `AE.CueConfig(tail_anchor_rescue=False, deep_intro_anchor=False, incoming_phrase_anchors=False)` (line 125) — required to reproduce the pre-D9 split, correctly documented.

(7) **Grep audit — nothing else assumes the old defaults.** All call sites of the three flags in `align_engine.py` and `propose_arrangement.py:2284-2294` (`_apply_cue_signals` mapping) gate on the flags correctly. `_mix_cues`, `_incoming_swap_anchors`, `_align_pair_landmark_aware`, `plan_fill_or_cut` all read the flags fresh — no path hardcodes the old defaults.

**MINOR finding — stale comment, not a defect.** SEVERITY: MINOR. Defect: `test_codex_blocker_fixes.py:116` annotates `AE.CUE_CONFIG = AE.CueConfig()` with `# every flag off`; after D9, `CueConfig()` has `tail_anchor_rescue=True, deep_intro_anchor=True, incoming_phrase_anchors=True` (not all off). Falsifier: read the comment after D9 and assume `plan_fill_or_cut` gates on those three flags — it doesn't, so the test still passes for the right reason (`incoming_intro_loop`/`emit_bass_out`/`emit_fills`/`emit_hint_fields`/`matched_tail_head_swap` are still OFF and those are the only flags `plan_fill_or_cut` reads, lines 2236/2261/976/986/996/1201), but the comment misrepresents what `CueConfig()` post-D9 means. One-line fix: replace `# every flag off` with `# every plan-relevant flag off (tail_anchor_rescue/deep_intro_anchor/incoming_phrase_anchors default ON since D9, but plan_fill_or_cut doesn't gate on them)`.

Full suite 874 passed/6 skipped/0 failed is consistent with this analysis — no behavioural change reaches a test the suites actually exercise.

===REVIEW-COMPLETE===


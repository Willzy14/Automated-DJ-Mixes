# C7 Step 1 review - Claude subagent standing in for Codex (capped until 2026-09-19)

## Review: Burn List C7 Step 1 — shadow_swap_preference + held-out evaluation

**1. Correctness of `shadow_swap_preference` + wiring — SOUND.** Similarity math (`Source/canonicalize_pair_history.py:379-397`) is line-for-line identical to `find_similar_pairs` in `propose_arrangement.py:1210-1237` (BPM 0.3/structure 0.7, threshold 0.1, max_results 3, same `_structure_signature`). `exclude_project` filters at the top of the scoring loop (`canonicalize_pair_history.py:384`) — complete exclusion, no off-by-one. I independently confirmed the wiring's `current_project = als_path.parent.parent.name` (`propose_arrangement.py:1598`) actually resolves to the correct project name by checking the real on-disk structure of three verifiable projects (`Test Project/<name>/Output/<file>.als` — two levels up from an ALS in `Output/` is exactly the project root, matching `pair_history.jsonl`'s `"project"` field). A repo-wide grep (gitignore-respecting) found `shadow_swap_preference` in only 6 files; its one write site (`propose_arrangement.py:2257-2260`) assigns into a JSON report dict only — never read by `align_pair`, `_search_anchors`, or the loop/trim-collection code that follows (verified by reading that whole block, `propose_arrangement.py:1604-1618`). Tests hand-verify real arithmetic (weighted average, exclusion, threshold, max_results) rather than restating the implementation.

**2. Evaluation harness — SOUND.** `evaluate()` calls `shadow_swap_preference(..., exclude_project=held_out.project)` on every row; `test_evaluate_never_lets_a_pair_predict_itself` directly proves no leakage. Tolerance sign/direction is correct and symmetric for both shadow and baseline (`abs(x - true_delta) <= 4.0`). The baseline comparison is fair — the "beats/loses" verdict uses `shadow_beats_baseline_on_covered`, the SAME covered subset for both, not the more favorable "overall" baseline number. Strongest check: I re-ran `evaluate_shadow_swap_preference.py` fresh against the real `pair_history.jsonl` and the output JSON is **byte-identical** to the staged file. I additionally hand-verified true_delta for 5 rows (Black Book pairs 1,2,6,8,9) directly against the raw JSONL duplicate records, and independently derived the 4 excluded conflicts (pairs 3,4,5,7 — matching the module's own docstring examples) and the 1 malformed record (line 38, `sam_bass_swap_beat: null`) — both exactly match the tool's reported "4 conflicts / 1 malformed."

**3. Burn list write-up — mostly matches, one real discrepancy.** "31 canonical pairs," "19% (6/31)," "68% (21/31)," "LOSES to baseline by 15 pairs" are exact matches. The ALS determinism claim is plausible and structurally sound (the new code only writes into the report dict, never touches ALS-writing/compression), though I didn't re-run the 3x regeneration myself. **Discrepancy: "Full suite 845 -> 851" (+6) doesn't reconcile.** `git diff` shows exactly 6 new tests in `test_canonicalize_pair_history.py` **and** 6 new tests in the brand-new `test_evaluate_shadow_swap_preference.py` — 12 new tests attributable to this work, not 6. Looks like a stale count from before the evaluate file was written, not updated in the final entry.

**Unasked finding:** the negative result looks trustworthy on its own terms — 68% of real corrections genuinely are zero-delta, and a similarity-weighted average structurally regresses toward small values, systematically missing the large real deltas (64, -52, -32, 56 beats) that matter. No evidence of a false negative or false positive in the harness.

VERDICT: CORRECTION — Documentation/BURN_LIST.md's "Full suite 845 -> 851 (6 new tests)" undercounts by half: git diff shows 6 new tests in Tests/test_canonicalize_pair_history.py plus 6 new tests in the new Tests/test_evaluate_shadow_swap_preference.py, i.e. 12 new tests attributable to C7 Step 1, not 6.

---
**Disposition (2026-09-16, Claude):** confirmed independently against the real test suite run
(`pytest Tests/ -q` -> 857 passed, matching 845+12 exactly, not the stale 851). Fixed in
BURN_LIST.md. MiniMax found the identical discrepancy independently, same specific number. No
other findings from either reviewer required a change - both confirmed the code, the wiring, the
evaluation harness's fairness, and the negative result itself are all sound.
